VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
VITE_API_BASE_URL=

# 站点对外根地址（canonical / og:url / og:image 的基址），必须带结尾斜杠。
# 默认值已写入 vite.config.ts 的 DEFAULT_SITE_URL（纳入版本控制，保证可复现）；
# 仅当需要临时切到别的域名（例如 ICP 备案后绑定的自定义域名）时才在此覆盖。
# VITE_SITE_URL=https://your-domain.example.com/workpreparation-01/
